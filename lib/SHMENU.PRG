bar=bar()
pad=Pad()
popup=Popup()
if ''#Menu()
	if ''#m.pad
		Show Menu (Menu()) Pad (m.pad)
	endif
	if m.bar#0 and ''#m.popup
		Show Popup (m.popup) Bar m.bar
	endif
endif
Close Data
exit=0
Return .t.